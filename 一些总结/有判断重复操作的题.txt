链表判重：
82，83

数组判重：
数组判重的重点在于，你曾使用了哪个元素，和你当前要使用的是哪个元素
** 我们要确保当前我们要使用的元素，和之前所使用的元素是不一样的就可以了
例如你曾使用了 i-1, 那么你就要进行 if nums[i-1] == nums[i]: continue
例如你曾使用了 i  , 那么你就要进行 if nums[i] == nums[i+1]: continue
26,80
15.3sum
40.combination sum2
